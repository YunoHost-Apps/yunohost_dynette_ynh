#!/usr/sbin/nft -f

table inet nat {
    chain prerouting {
        type nat hook prerouting priority dstnat
        iifname "__IFACE__" meta l4proto { tcp, udp } th dport 53 redirect to :1053
    }
}
